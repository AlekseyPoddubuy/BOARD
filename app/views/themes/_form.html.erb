
<%= simple_form_for(@theme) do |f| %>
<%= f.error_notification %>

<div class="form-inputs">
	<%= f.input :theme %>
</div>

<% if has_role?(:admin) %>
<div class="form-inputs">
	<%= f.select :status, options_for_select([ ["Закрыть", "1"], ["Опубликовать", "0"]]) %>
</div>
<% else %>


<% end %>


<div class="form-actions">
	<%= f.button :submit %>
</div>
<% end %>
