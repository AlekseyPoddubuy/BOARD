<%= notice %>
<div class="agencies">
  <div class="container">
    <div class="row">
      <div class="col-lg-12">
        <div class="trigger">
          <h1>Всеукраинский рейтинг фирм по трудоустройству</h1>
        </div>
      </div>
      <div class="col-lg-12">
        <div class="gold-top">

        </div>
      </div>

      <% @agencies.each do |agency| %>

      <div class="col-lg-3">
        <% if agency.rate < 10 %>
        <div class="agency-top">
          <%= image_tag "crown.png", class: "agn-crown"  %>
          <% else %>          
          <div class="agency">
            <% end %>
            <%= image_tag agency.avatar.url, class: 'agn-logo' %>
            <p class="agn-description"><%= agency.description %></p>
            <p class="agn-title"><%= agency.title %></p>
            <%= link_to "Детальней", agency, class: 'btn btn-warning' %>
          </div>
        </div>
        <% end %>
      </div>
    </div>
  </div>


  <h1>Agencies</h1>

  <table>
    <thead>
      <tr>
        <th>Title</th>
        <th>Description</th>
        <th>Body</th>
        <th>Rating</th>
        <th>Datetime</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @agencies.each do |agency| %>
      <tr>
        <td><%= agency.title %></td>
        <td><%= agency.description %></td>
        <td><%= agency.body %></td>
        <td><%= agency.rating %></td>
        <td><%= agency.datetime %></td>
        <td><%= link_to 'Show', agency %></td>
        <td><%= link_to 'Edit', edit_agency_path(agency) %></td>
        <td><%= link_to 'Destroy', agency, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to 'New Agency', new_agency_path %>
-->