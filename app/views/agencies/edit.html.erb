<% if has_role?(:admin) %>

<h1>Editing Agency</h1>

<%= render 'form', agency: @agency %>

<%= link_to 'Show', @agency %> |
<%= link_to 'Back', agencies_path %>

<% else %>

<%=  render "components/accsess" %>

<% end %>