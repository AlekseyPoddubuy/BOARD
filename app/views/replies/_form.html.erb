<%= simple_form_for([@topic, @topic.replies.build]) do |f| %>

  <div class="form-inputs">
    <%= f.input :reply %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, 'Leave reply', class: 'btn btn-info' %>
  </div>
<% end %>
