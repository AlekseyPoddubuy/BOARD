<div class="col-lg-12">
	<hr>
	<%= reply.user.username %> <%= time_ago_in_words(reply.created_at) %> 


	<% if reply_author(reply) %>
	<%= link_to [reply.topic, reply], method: :delete, data: { confirm: 'Are you sure?' } do %> Удалить комментарий<% end %> 

	<% end %>
	<br>
	
	<div class="reply">
		<%= image_tag "stars.png", style: 'width: 70px;' %> <p><%= reply.reply %></p>
	</div>
</div>